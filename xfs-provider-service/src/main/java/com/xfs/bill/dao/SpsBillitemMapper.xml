<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SPS_BILLITEM" > 
<!-- Result Map-->
<resultMap id="result_map_SPS_BILLITEM" type="com.xfs.bill.model.SpsBillitem" >
	<result column="id" property="id"/>
	<result column="bill_id" property="billId"/>
	<result column="name" property="name"/>
	<result column="fee" property="fee"/>
	<result column="source" property="source"/>
</resultMap>
       
<!-- sps_billitem table all fields -->
<sql id="Base_Column_List" >
	 id,bill_id,name,fee,source
</sql>

<!-- 查询条件 -->
<sql id="Example_Where_Clause">
  <trim prefix=" WHERE " prefixOverrides="AND ">
	<if test="id != null" >and id = #{id}</if>
	<if test="billId != null" >and bill_id = #{billId}</if>
	<if test="name != null" >and name = #{name}</if>
	<if test="fee != null" >and fee = #{fee}</if>
	<if test="source != null" >and source = #{source}</if>
</trim>
</sql>

<!-- 查询条件 带模糊查询 以及 时间起始查询 -->
<sql id="Example_Where_Clause2">
  <trim prefix=" WHERE " prefixOverrides="AND ">
    <if test="id != null">AND id = #{id}</if>
    <if test="billId != null">AND bill_id = '${billId}'</if>
    <if test="name != null">AND name like '%${name}%'</if>
    <if test="nameEq != null">AND name = #{nameEq}</if>
    <if test="fee != null">AND fee like '%${fee}%'</if>
    <if test="source != null">AND source like '%${source}%'</if>
  </trim>
</sql>

<!-- 插入记录 -->
<insert id="InsertSPS_BILLITEM" parameterType="Object" >
  INSERT INTO SPS_BILLITEM (
  <trim prefix=" " prefixOverrides=",">
	<if test="id != null ">,id</if>
	<if test="billId != null ">,bill_id</if>
	<if test="name != null ">,name</if>
	<if test="fee != null ">,fee</if>
	<if test="source != null ">,source</if>
  </trim>
  )  VALUES (
  <trim prefix=" " prefixOverrides=",">
	<if test="id != null ">,#{id}</if>
	<if test="billId != null ">,#{billId}</if>
	<if test="name != null ">,#{name}</if>
	<if test="fee != null ">,#{fee}</if>
	<if test="source != null ">,#{source}</if>
  </trim>
  )
  <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
	SELECT LAST_INSERT_ID() AS ID
  </selectKey>
</insert>

<!-- 根据id，修改记录-->  
<update id="UpdateSPS_BILLITEM" parameterType="Object" >
UPDATE SPS_BILLITEM 
  <trim prefix=" SET " prefixOverrides=",">
	<if test="id != null ">,id=#{id}</if>
	<if test="billId != null ">,bill_id=#{billId}</if>
	<if test="name != null ">,name=#{name}</if>
	<if test="fee != null ">,fee=#{fee}</if>
	<if test="source != null ">,source=#{source}</if>
  </trim>
WHERE ID=#{id}
</update>
 
<!-- 删除记录 -->
<delete id="DeleteSPS_BILLITEM">DELETE FROM SPS_BILLITEM WHERE ID=#{id}</delete>
<!-- 账单项目 列表总数-->
<select id="CountFindAllSPS_BILLITEM" resultType="java.lang.Integer">SELECT count(1) as value FROM SPS_BILLITEM</select>
<select id="CountSPS_BILLITEM" resultType="java.lang.Integer">SELECT count(1) as value FROM SPS_BILLITEM WHERE ID=#{id}</select>
<!-- 根据id查询 账单项目 -->
<select id="FindByPK" resultMap="result_map_SPS_BILLITEM" parameterType="Object">SELECT <include refid="Base_Column_List" /> FROM SPS_BILLITEM WHERE ID=#{id}</select>

<!-- 根据各字段条件带模糊查询  账单项目 -->
<select id="FreeFindSPS_BILLITEM" resultMap="result_map_SPS_BILLITEM" parameterType="Object">
	SELECT <include refid="Base_Column_List" /> FROM SPS_BILLITEM 
	 <include refid="Example_Where_Clause2" />
</select>
<!-- 根据各字段条件带模糊查询  账单项目 记录数 -->
<select id="CountFreeFindSPS_BILLITEM" resultType="java.lang.Integer">
SELECT count(1) as value FROM SPS_BILLITEM 
 <include refid="Example_Where_Clause2" />
</select>
<!-- 根据各字段条件带排序  账单项目 -->
<select id="FreeFindSPS_BILLITEMOrdered" resultMap="result_map_SPS_BILLITEM" parameterType="Object">
	SELECT <include refid="Base_Column_List" /> FROM SPS_BILLITEM 
	 <include refid="Example_Where_Clause2" />
	 ORDER BY ${_orderBy}
</select>

<!-- *****************************  温馨提示：  ***************************** -->
<!-- **                                                                    ** -->
<!-- ** 以上为自动生成代码，请勿改动或删除。如有新sql添加请在下方区域内填写** -->
<!-- **                                             ** 2016/05/16 15:32:12 ** -->
<!-- ************************************************************************ -->

	<!--查询企业账单项目-->
	<select id="QUERY_BILL_ITEMS_BY_PARAMS" resultMap="result_map_SPS_BILLITEM" parameterType="Object">
		SELECT * FROM sps_billitem item WHERE EXISTS (
			SELECT t.bill_id FROM (
			  SELECT b.bill_id FROM sps_bill b WHERE b.cp_id = #{cmid}
		     <if test="null != spid "> AND b.sp_id = #{spid}</if>
		     <if test="null == spid "> AND b.bill_state = 'SEND'</if>
		     <if test="null != bill_id "> AND b.bill_id = #{bill_id}</if>
             <if test="period != null "> AND b.period = #{period} </if>
             AND b.dr = 0
			  ORDER BY b.bill_id desc LIMIT 1
		 ) t WHERE t.bill_id = item.bill_id
		)
	</select>


</mapper>   
